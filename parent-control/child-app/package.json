{
  "name": "mdm-agent",
  "version": "1.0.0",
  "description": "MDM Agent - transparent device management client",
  "main": "index.js",
  "scripts": {
    "sync": "npx cap sync",
    "open": "npx cap open android",
    "build:apk": "npx cap sync android && cd android && .\\gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
